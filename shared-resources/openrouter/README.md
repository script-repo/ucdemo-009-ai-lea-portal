# OpenRouter

Fallback hosted inference. Used only when Nutanix Enterprise AI fails
or is not configured.

Create a key at https://openrouter.ai/keys and save it on
`/#/resources`. Do not put the key in Git or in a Kubernetes Secret
for this lab.
