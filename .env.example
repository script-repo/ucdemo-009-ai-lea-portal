# Optional build-time URLs for non-chat services (vector, objects, K8s).
# Chat completions do NOT use these — configure Nutanix Enterprise AI
# and OpenRouter on the Resources page. Keys stay in the browser.

VITE_AISP_INFERENCE_URL=
VITE_AISP_VECTOR_URL=
VITE_AISP_RELATIONAL_URL=
VITE_AISP_OBJECT_STORAGE_URL=
VITE_AISP_VIRTUALIZATION_URL=
VITE_AISP_KUBERNETES_URL=
